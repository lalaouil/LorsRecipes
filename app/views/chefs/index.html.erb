<%= render 'shared/page_title', title: 'All Chefs' %>

<div class="container">
  <ul class="nav nav-tabs">
    <li>
      <%= link_to "All Recipes", recipes_path %>
    </li>
    <li class="active">
      <%= link_to "All Chefs", chefs_path %>
    </li>
  </ul>
   <div class="row">
    <p>&nbsp</p>
    <p>&nbsp</p>
  </div>
</div>

<div class="container">
  <% @chefs.each do |chef| %>
  <div class= "col-lg-6 col-lg-6 center">
  <%= link_to gravatar_for(chef, size: 200), chef_path(chef) %>
    <p>
      <h5>Chef name: <%= chef.chefname %></h5>
    </p>
    <p>&nbsp</p>
  </div>
<% end %>
</div>
  <div class="paginate center">
    <%= will_paginate %>
  </div>
